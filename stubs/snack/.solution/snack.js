
/**
 * Add the solution to your snack and check if the tests are passing.
 * Only snack with passing tests will be accepted.
 * @returns 
 */
const solution = () => {
    return
};


export default { solution: solution };